# Infrastructure

This folder will contain adapters for browser-local persistence, PDF generation, backup/restore, current-user handling, and future Ezsked integration.

Phase 1 intentionally does not implement IndexedDB, PDF generation, network calls, authentication, or CRM/email integrations.
